*******************************************************
ajax같이 동적으로 불러와지는 것들은 일반적으로 쓰는 click이벤트가 먹히지 않기 때문에 아래와 같이 쓴다
*******************************************************

$(document).on('click', '.alertBtn', function(){
		alert('click!');
	}